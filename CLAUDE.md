# CLAUDE.md — 이 저장소에서 작업하는 모든 Claude 세션이 지킬 규칙

이 파일은 Claude Code 가 세션마다 자동으로 읽는다. 아래 규칙은 기본 동작보다
우선한다.

## 0. 한 줄 요약

**SELAH RTA** — 교회 예배당용 안드로이드 음향 분석 앱(Kotlin · Compose).
SPL·RTA·하울링 탐지·마이크 교정. 저장소는 `janghorse-boop/SELAH-RTA` 하나뿐이고
**SVT ONE 저장소에 절대 올리지 않는다.**

- 역할: Claude 가 개발 담당, Codex 가 독립 검토자(명세 24장)
- 명세·결정·측정 기록은 `docs/` 에 있다. 비자명한 결정은 **까닭을 먼저 찾는다** —
  `docs/decisions/` 에 그 까닭이 적혀 있다.

## 1. 커밋과 푸시

- 커밋 메시지는 한국어로, **무엇을 왜** 고쳤는지 적는다. 마지막 줄에:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
- **푸시는 사람이 한다.** Claude 는 로컬 커밋까지만 만든다.
- 새 가지는 `git push -u origin <가지이름>` 으로 한 번 올릴 곳을 정해 줘야 한다.

## 2. `inbox/` 는 저장소에 올리지 않는다

사용자가 파일을 건네주는 자리다(`.gitignore`). 명세 원본, 개별 CAL 파일,
검토 산출물이 들어온다. **참고할 것은 `docs/` 로 옮겨 커밋한다** — 원본을
`inbox/` 에 둔 채 커밋하지 않는다.

실제 EMM-6 CAL 파일(`inbox/17860.txt`)은 일부러 커밋하지 않았다. 형식만
본떠 시험과 문서에 옮겼다.

## 3. 소스 편집

- **Edit/Write 도구로 편집한다.** PowerShell `Set-Content`/`Out-File` 로 소스를
  고치지 않는다 — 한글/UTF-8 이 깨진다.
- 모든 UI 문구는 한국어. 파일은 UTF-8.

## 4. 재는 코드를 고쳤으면 재서 확인한다

- 짐작하지 말고 **실제로 돌려 본다.** 화면만 보고 고친 것은 세 번 헛돌았다.
- **문턱값을 올려 통과시키지 않는다.** 허용치를 늘리는 것은 코드 대신 관문을
  고치는 일이다. 재는 쪽이 틀렸는지부터 본다(실제로 핑크 노이즈 파일의
  이음매가 원인이었던 적이 있다 — `docs/spec/2026-09-24-response-curve-attempt.md`).
- 변이 시험을 돌릴 때는 **반드시 `--rerun-tasks`** 를 붙인다. Gradle 빌드
  캐시가 옛 컴파일 결과를 조용히 내주어 변이가 살아남은 것처럼 보인다.
- 빌드·시험: `./gradlew :app:assembleDebug :app:testDebugUnitTest :dsp:test`
  (JDK 는 Android Studio 의 `jbr`).

## 5. 가격·구독 — **코드에 박지 않는다**

가격 전략은 [docs/business/2026-09-24-pricing-subscription-strategy.md](docs/business/2026-09-24-pricing-subscription-strategy.md)
에 있다. 그 문서 8장이 정한 것을 그대로 옮긴다.

- 그 문서는 **검토용 초안**이다. 결제 활성화·스토어 상품 등록·가격 확정을
  승인하는 문서가 **아니다.**
- **가격·체험 기간·상품명·기능 권한을 확정 요구사항으로 하드코딩하지 않는다.**
  4,900원·49,000원·14일·5,900원·8,900원은 전부 가설이다. 출시 결정 뒤 승인된
  스토어 상품 설정과 연결한다.
- 요금제 분기·기능 잠금·결제 화면을 **지시 없이 만들지 않는다.**
- 「계획된 기능」과 「출시 완료 기능」을 구분한다. 미완성 고급 기능
  (Transfer Function·Phase·Coherence·Delay)을 판매 설명이나 UI 약속에 넣지 않는다.

## 6. 측정 마이크를 품질 등급으로 나누지 않는다

전략 문서 3장 「측정 마이크 표현 원칙」이다. **UI·문서·주석 모두에 적용된다.**

- **iMM-6C 와 EMM-6 은 둘 다 개별 CAL 을 적용하는 정식 측정용 마이크다.**
- 「간편형/정밀형」, 「입문용/전문용」, 「보급형/고급형」 같은 **등급 표현을
  쓰지 않는다.** 연결 단자만으로 우열을 단정하지 않는다.
- 다른 것은 **연결 방식**뿐이다 — `ReferenceHookup` 참고.
  XLR+인터페이스에는 팬텀전원과 게인 노브가 있고, USB 직결에는 없다.
  그래서 마법사 1단계가 확인하는 것도 다르다.
- 최대 SPL 여유는 마이크와 **전체 입력 경로**의 실제 게인·클리핑으로 검증한다.
- 멀티채널·Transfer Function 은 인터페이스 동시 입력·기준 신호 경로·앱 지원이
  모두 갖춰져야 한다. **EMM-6 을 꽂았다는 것만으로 그 기능이 되지 않는다.**
- 주파수 보정파일 적용과 **절대 SPL 교정은 다른 일이다.** 섞어 쓰지 않는다.

## 7. 말하지 않은 것을 말한 것처럼 적지 않는다

이 저장소가 거듭 데인 자리다.

- **권장 범위는 보편적 표준이 아니다.** 참고값이고 사용자가 고칠 수 있으며,
  공식 청력 안전기준과 같게 보지 않는다(명세 18장).
- **RTA 스펙트럼과 전달함수(Transfer Function) 측정을 혼동하는 설명을 쓰지
  않는다.** 지금 앱은 단일 채널 RTA 다.
- 「보정됨」은 **그 기준과 맞다**는 뜻이지 절대 음압이 추적 가능하다는 뜻이
  아니다. 무엇에 맞춘 보정인지 `CalibrationSource` 로 남기고 화면에 적는다.
- 안드로이드가 주는 이름(`AudioDeviceInfo.getAddress()`)은 **표기일 뿐 실제
  물리 위치가 아니다.** S23 Ultra 는 상단 마이크를 `back` 이라 알린다.
